[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "barrot-lattice"
version = "0.1.0"
description = "Barrot: a multi-agent cognitive lattice architecture"
requires-python = ">=3.10"
dependencies = ["pyyaml","rich","typer"]
